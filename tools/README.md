# Regenerar faq.json

Si editas `faq_source.md`, vuelve a generar el JSON con:

```
python3 md_to_json.py
```

Esto crea/actualiza `faq.json` en esta misma carpeta. Después cópialo a
`app/src/main/assets/faq.json`.
