# Chatbot_Istg
Inferencia local (on-device)**: el modelo de lenguaje (Gemma 3 1B,    cuantizado) corre directamente en el procesador del teléfono mediante    MediaPipe LLM Inference API — no se envía ningún dato a servidores externos    para generar la respuesta.
